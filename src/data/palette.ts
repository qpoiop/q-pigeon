/**
 * Modernist palette — the single source of truth for the game's colours.
 * bg #f3f2f2 (ground) · ink #201e1d (text/walls) · accent #ec3013 (objectives).
 * A mono scheme: one accent, the rest are neutral tints of the ink.
 */
export const INK = 0x201e1d;
export const BG = 0xf3f2f2;
export const ACCENT = 0xec3013;
export const MID = 0x8a8683;
export const PAPER = 0xe9e7e5;

/** CSS hex strings for the same roles, used in HUD/DOM. */
export const CSS_INK = '#201e1d';
export const CSS_BG = '#f3f2f2';
export const CSS_ACCENT = '#ec3013';
export const CSS_ACCENT_PRESSED = '#c92a10';
